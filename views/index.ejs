<%- include('partials/header') %>
    <div class="table-container">
        <h1  class="todo-title">To Do</h1>
        <div class="">
            <a class="btn" href="/add-new-task">Add a task</a>
            <br>
            <br>
            <table>
                <tr>
                    <th>Task no.</th>
                    <th>Task title</th>
                    <th>Task description</th>
                    <th>Created at</th>
                    <th>Updated at</th>
                    <th>Action</th>
                </tr>
                <% for(let i=0; i < todos.length; i++){%>
                    <tr>    
                        <td><%= i+1 %></td>
                        <td><%= todos[i].title %></td>
                        <td><%= todos[i].desc %></td>
                        <td><%= moment(todos[i].createdAt).format("MMM DD, YYYY HH:mm:ss") %></td>
                        <td><%= moment(todos[i].updatedAt).format("MMM DD, YYYY HH:mm:ss") %></td>
                        <td>
                            <a class="btn" href="/update-task">edit</a>
                            <br><br>
                            <a class="btn" href="/delete-task">delete</a>
                        </td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
<%- include("partials/footer") %>